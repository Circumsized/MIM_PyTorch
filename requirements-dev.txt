-r requirements.txt

# Testing
pytest>=9.0.3
hypothesis>=6,<7

# Linting / static analysis
ruff>=0.6

# Security audit
pip-audit>=2

# Build backend (kept current to avoid known CVEs)
setuptools>=83.0.0
